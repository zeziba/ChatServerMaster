sudo: false

language: python

python:
  - 3.6

before_script:
- pip install coveralls coverage setuptools

install:
  - pip install -r requirements.txt

script:
- pytest --with-coverage ConfigManger/
- pytest --with-coverage Server/
- pytest --cov=ChatServerMaster tests/

after_success:
- coveralls
sudo: false

language: python

python:
  - 3.6

before_script:
- pip install coveralls coverage setuptools

install:
  - pip install -r requirements.txt

script:
- python -m pytest ./tests/*
- coverage run --source ChatServerMaster -m unittest discover && coverage report

after_success:
- coveralls